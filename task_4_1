def function(x, y):
    n = 1
    b = 1
    while n <= abs(y):
        n += 1
        b = 1 / x * b
    print(b)


k = float(input("Введите число "))
l = float(input("Введите степень "))
print(function(k, l))
